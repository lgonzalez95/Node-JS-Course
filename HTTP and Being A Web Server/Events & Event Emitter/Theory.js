/**
 * We have system events: They are related with events that comes from the computer system. 
 * Done by C++ Core. IE: Reading a file
 * 
 * 
 * We also have custom events: They are inside JS core. I can create them by myself, 
 * it's done by the event emitter
 */

//Magic String: A string that has some special meaning in our code